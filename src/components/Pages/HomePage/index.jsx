import './styles.css';
import {Tweet} from '../../Tweet';

export const HomePage = () => {
    return(
        <div>
            <h1>Home page (currently testing)</h1>
            <Tweet/>

        </div>
    );
}